
const host = 'https://wwikpzxt.qcloud.la';
const reqUrl = host + '/api'

module.exports  = {

    // 登录地址
    loginUrl: `${host}/login`,

    getUserInfo:`${reqUrl}/me`,

    getItemDetail: `${host}/item`,
};

